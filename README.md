CurseSMP-clean - Clean plugin project

This folder contains a prepared Gradle project with decompiled sources copied from the original.

To build:

```bash
cd server/plugins/CurseSMP-clean
./gradlew build
```

Resulting jar: `build/libs/CurseSMP-clean-1.0.0.jar`
